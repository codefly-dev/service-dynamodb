# DynamoDB Local

This plugin runs a pinned DynamoDB Local service for development, testing, and
Kubernetes environments. It exports the selected endpoint and AWS region as
configuration for dependent services.

Production applications that use AWS DynamoDB should replace the local workload
through an environment overlay and inject the managed AWS endpoint/credentials;
this plugin does not pretend SQL migrations apply to DynamoDB.
